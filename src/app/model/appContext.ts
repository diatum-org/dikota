export class AppContext {
  amigoId: string;
  registry: string;
  token: string;        // token to communicate with account node server
  appNode: string;
  appToken: string;     // token to communicate with dikota node server
  serviceToken: string; // token to communicate with dikota app server
}

